#média
print( sum(5,6,7,1,2,4,3,8)/len(5,6,7,1,2,4,3,8) )

#>3
print(str.join (map(lambda x: str(x), filter(lambda x: x > 3, 5,6,7,1,2,4,3,8))))

#<5
print(str.join (map(lambda x: str(x), filter(lambda x: x < 5, 5,6,7,1,2,4,3,8))))

#soma
sum(5,6,7,1,2,4,3,8)

#;
print(str.join(";",map (lambda x: str(x),lista)))

#
print(int.join("-" , map(lambda x: x*-1, lista)))
